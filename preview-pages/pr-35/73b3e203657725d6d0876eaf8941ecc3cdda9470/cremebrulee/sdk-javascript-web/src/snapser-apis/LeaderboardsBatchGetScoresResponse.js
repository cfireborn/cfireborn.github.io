/**
 * cremebrulee
 * Your custom SDK
 *
 * The version of the OpenAPI document: cremebrulee: v1 SDK
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient.js';
import LeaderboardsBatchGetScoresSingleResponse from './LeaderboardsBatchGetScoresSingleResponse.js';
import LeaderboardsTieredUserScore from './LeaderboardsTieredUserScore.js';

/**
 * The LeaderboardsBatchGetScoresResponse model module.
 * @module snapser-apis/LeaderboardsBatchGetScoresResponse
 * @version cremebrulee: v1 SDK
 */
class LeaderboardsBatchGetScoresResponse {
    /**
     * Constructs a new <code>LeaderboardsBatchGetScoresResponse</code>.
     * @alias module:snapser-apis/LeaderboardsBatchGetScoresResponse
     */
    constructor() { 
        
        LeaderboardsBatchGetScoresResponse.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>LeaderboardsBatchGetScoresResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:snapser-apis/LeaderboardsBatchGetScoresResponse} obj Optional instance to populate.
     * @return {module:snapser-apis/LeaderboardsBatchGetScoresResponse} The populated <code>LeaderboardsBatchGetScoresResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new LeaderboardsBatchGetScoresResponse();

            if (data.hasOwnProperty('responses')) {
                obj['responses'] = ApiClient.convertToType(data['responses'], [LeaderboardsBatchGetScoresSingleResponse]);
            }
            if (data.hasOwnProperty('user_scores')) {
                obj['user_scores'] = ApiClient.convertToType(data['user_scores'], [LeaderboardsTieredUserScore]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>LeaderboardsBatchGetScoresResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>LeaderboardsBatchGetScoresResponse</code>.
     */
    static validateJSON(data) {
        if (data['responses']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['responses'])) {
                throw new Error("Expected the field `responses` to be an array in the JSON data but got " + data['responses']);
            }
            // validate the optional field `responses` (array)
            for (const item of data['responses']) {
                LeaderboardsBatchGetScoresSingleResponse.validateJSON(item);
            };
        }
        if (data['user_scores']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['user_scores'])) {
                throw new Error("Expected the field `user_scores` to be an array in the JSON data but got " + data['user_scores']);
            }
            // validate the optional field `user_scores` (array)
            for (const item of data['user_scores']) {
                LeaderboardsTieredUserScore.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * List of responses with the latest scores
 * @member {Array.<module:snapser-apis/LeaderboardsBatchGetScoresSingleResponse>} responses
 */
LeaderboardsBatchGetScoresResponse.prototype['responses'] = undefined;

/**
 * @member {Array.<module:snapser-apis/LeaderboardsTieredUserScore>} user_scores
 */
LeaderboardsBatchGetScoresResponse.prototype['user_scores'] = undefined;






export default LeaderboardsBatchGetScoresResponse;

