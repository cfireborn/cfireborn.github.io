/**
 * cremebrulee
 * Your custom SDK
 *
 * The version of the OpenAPI document: cremebrulee: v1 SDK
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient.js';
import LeaderboardsBatchIncrementScoreSingleResponse from './LeaderboardsBatchIncrementScoreSingleResponse.js';
import LeaderboardsIncrementScoreResponse from './LeaderboardsIncrementScoreResponse.js';

/**
 * The LeaderboardsBatchIncrementScoreResponse model module.
 * @module snapser-apis/LeaderboardsBatchIncrementScoreResponse
 * @version cremebrulee: v1 SDK
 */
class LeaderboardsBatchIncrementScoreResponse {
    /**
     * Constructs a new <code>LeaderboardsBatchIncrementScoreResponse</code>.
     * @alias module:snapser-apis/LeaderboardsBatchIncrementScoreResponse
     */
    constructor() { 
        
        LeaderboardsBatchIncrementScoreResponse.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>LeaderboardsBatchIncrementScoreResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:snapser-apis/LeaderboardsBatchIncrementScoreResponse} obj Optional instance to populate.
     * @return {module:snapser-apis/LeaderboardsBatchIncrementScoreResponse} The populated <code>LeaderboardsBatchIncrementScoreResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new LeaderboardsBatchIncrementScoreResponse();

            if (data.hasOwnProperty('increment_score_responses')) {
                obj['increment_score_responses'] = ApiClient.convertToType(data['increment_score_responses'], [LeaderboardsIncrementScoreResponse]);
            }
            if (data.hasOwnProperty('responses')) {
                obj['responses'] = ApiClient.convertToType(data['responses'], [LeaderboardsBatchIncrementScoreSingleResponse]);
            }
            if (data.hasOwnProperty('scores_failed')) {
                obj['scores_failed'] = ApiClient.convertToType(data['scores_failed'], 'Number');
            }
            if (data.hasOwnProperty('scores_incremented')) {
                obj['scores_incremented'] = ApiClient.convertToType(data['scores_incremented'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>LeaderboardsBatchIncrementScoreResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>LeaderboardsBatchIncrementScoreResponse</code>.
     */
    static validateJSON(data) {
        if (data['increment_score_responses']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['increment_score_responses'])) {
                throw new Error("Expected the field `increment_score_responses` to be an array in the JSON data but got " + data['increment_score_responses']);
            }
            // validate the optional field `increment_score_responses` (array)
            for (const item of data['increment_score_responses']) {
                LeaderboardsIncrementScoreResponse.validateJSON(item);
            };
        }
        if (data['responses']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['responses'])) {
                throw new Error("Expected the field `responses` to be an array in the JSON data but got " + data['responses']);
            }
            // validate the optional field `responses` (array)
            for (const item of data['responses']) {
                LeaderboardsBatchIncrementScoreSingleResponse.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * @member {Array.<module:snapser-apis/LeaderboardsIncrementScoreResponse>} increment_score_responses
 */
LeaderboardsBatchIncrementScoreResponse.prototype['increment_score_responses'] = undefined;

/**
 * List of responses for each user ID
 * @member {Array.<module:snapser-apis/LeaderboardsBatchIncrementScoreSingleResponse>} responses
 */
LeaderboardsBatchIncrementScoreResponse.prototype['responses'] = undefined;

/**
 * @member {Number} scores_failed
 */
LeaderboardsBatchIncrementScoreResponse.prototype['scores_failed'] = undefined;

/**
 * @member {Number} scores_incremented
 */
LeaderboardsBatchIncrementScoreResponse.prototype['scores_incremented'] = undefined;






export default LeaderboardsBatchIncrementScoreResponse;

